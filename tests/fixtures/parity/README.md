# Parity fixtures

This directory contains curated fixtures used to prove behavioral parity by replaying captured/normalized protocol events.

## index.json

`index.json` is the manifest for fixture curation.

- `domains`: mapping of parity domain name to metadata.

Domain names are stable identifiers (e.g., `messages-recv`, `app-state-sync`).
